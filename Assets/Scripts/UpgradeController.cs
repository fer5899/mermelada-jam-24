using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpgradeController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Primero de todo debemos obtener las dos cartas que se van a enseñar
        //En segundo lugar debemos enseñar esas cartas que hemos obtenido y también las cartas que tenemos del mazo
        //Lo siguiente es esperar a que el jugador escoga dos cartas
        //Una vez las haya seleccionado debemos modificar su mazo en caso de que lo haya hecho
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
